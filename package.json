{
  "name": "root",
  "private": true,
  "devDependencies": {
    "i": "^0.3.6",
    "lerna": "^3.22.1",
    "npm": "^6.14.8",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build:aisdata": "lerna exec --scope aisdata npm run build:docker",
    "build:aisdata-collector-kystverket": "lerna exec --scope aisdata-collector-kystverket npm run build:docker",
    "build:common": "lerna exec --scope @redningsselskapet/rs-tracker-services-common npm run build",
    "build:all": "run-p build:aisdata build:aisdata-collector-kystverket build:common"
  }
}
